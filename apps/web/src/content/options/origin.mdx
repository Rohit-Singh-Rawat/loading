## Origin

Controls where the bars grow from. `center` scales them from the middle, `bottom` keeps their base fixed so they rise from the baseline. The default origin is `center`.

<Demo name="origin" />
