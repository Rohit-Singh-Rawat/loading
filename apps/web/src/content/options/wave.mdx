## Wave

Controls which way the wave runs through the grid. `diagonal` goes from the top left corner to the bottom right, `rows` from top to bottom, `columns` from left to right. The default wave is `diagonal`.

<Demo name="wave" />
