## Direction

Sets which way the lit cells sweep across the grid. `rows` goes top to bottom, `columns` left to right, and `diagonal` from the top-left corner down. The default value is `rows`

<Demo name="direction" />
