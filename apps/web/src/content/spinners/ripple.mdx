```tsx showLineNumbers
import { Ripple } from "loading-dev";

export function RippleDemo() {
  return <Ripple size={20} />;
}
```

## Customization

Use the `size` prop for the spinner's dimensions — the cells and the gaps
between them scale proportionally. Tint it with the CSS `color` property
via `currentColor`. Motion is controlled with CSS variables:
`--ld-duration` sets the time for one full pulse and `--ld-play-state`
pauses or resumes the animation.

## Usage

The pulse starts at the center and pushes outward, then rests for a beat
before repeating. The pause makes it calmer than a continuous spinner, so it
works for background work the user is not waiting on. Set the variables on
any wrapper — they cascade down to the spinner.

```tsx title="background-task.tsx"
<div className="flex items-center gap-2 text-gray-500">
  <Ripple size={16} />
  Listening for changes…
</div>
```
