## Size

Sets the spinner's dimensions in pixels. The default value is `20`

<Demo name="size" slug={props.slug} />

## Custom Classes

You can use custom classNames in case you need to tweak the spinner in a specific way.

<Demo name="class-name" slug={props.slug} />

## Color

The loader inherits `currentColor` parent by default. You can tint it in any way you want. It accepts any valid CSS color.

<Demo name="color" slug={props.slug} />

## Duration

Sets the speed in milliseconds. Higher values are slower, smaller values are faster.

<Demo name="duration" slug={props.slug} />

## State

Pauses or resumes the animation and exposes the state of the spinner.

<Demo name="play-state" slug={props.slug} />
