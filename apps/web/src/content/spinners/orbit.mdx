## Easing

Sets how the arc travels around the dot. `linear` keeps a constant speed, `ease-in-out` surges through each turn and pauses at its end, and `stacked` layers the two so the arc surges without ever coming to rest. The default value is `linear`

<Demo name="easing" />
